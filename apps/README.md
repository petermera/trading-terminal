# Apps

A basic, framework-neutral structure for pages, reusable sections, documentation, and shared assets.

## Folders

- `pages/` — complete app pages
- `sections/` — reusable page sections
- `documentation/` — project notes and guides
- `assets/` — shared images and styles

Start with `pages/home/README.md`, then add or reuse content from `sections/`.
