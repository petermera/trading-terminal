# Getting started

1. Create a folder under `pages/` for each new page.
2. Put reusable page parts under `sections/`.
3. Put shared images and styles under `assets/`.
4. Update the documentation when the structure changes.
